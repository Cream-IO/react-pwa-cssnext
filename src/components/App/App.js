import React from 'react';
import styles from './App.css';

const App = () => (
  <section className={styles.App}>
    <header className={styles.AppHeader}>
      <h1>React PWA with Offline features & cssnext</h1>
    </header>
    <p className={styles.AppIntro}>App is ready</p>
  </section>
);

export default App;
